# Supabase V2 — projeto exclusivo do BuildSmart V2 (não compartilhado com a V1)
# Copie este arquivo para .env.local e preencha com os valores reais do projeto.
# Nunca commite .env.local (já está no .gitignore).

# URL do projeto Supabase V2 (Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=

# Chave publicável/anon do projeto Supabase V2 (Project Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
